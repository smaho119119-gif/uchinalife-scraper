/**
 * Legacy module — re-exports from `lib/db.ts` for backward compatibility.
 * Prefer importing from `@/lib/db` (or `@/lib/types` for types only) in new code.
 * This module will be removed in a future round once all imports are migrated.
 */
export * from '@/lib/db';
